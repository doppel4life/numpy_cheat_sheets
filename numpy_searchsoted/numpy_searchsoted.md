# `numpy.searchsorted()`

## Description

Returns the index(es) where a value should be inserted into a **sorted array**
to maintain order.

---

## Syntax

```python
numpy.searchsorted(a, v, side='left')
